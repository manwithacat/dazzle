# HaTchi-MaXchi — review point (2026-07-03)

One day of oracle-driven work, baseline to now. Everything below is judged
by the same blind protocol (3 sonnet judges, identity-stripped 1440×900
frames, dialect references interleaved; within-run gaps are the metric).

## The tokens prototype

`/tmp` specimen (foundations rendered from the SHIPPING dist bundle, light
+ dark): Geist/Geist Mono type scale with tabular numerals, OKLCH neutral
+ brand + semantic ramps (600s = the WCAG-gated on-wash steps), badge
colour+icon+text vocabulary, button set with focus rings and press states,
stacked-shadow elevation, radius + spacing scales, designed empty state,
KPI card, and the 140-icon vendored Lucide registry — all server-rendered,
no client icon hydration. Regenerate any time:
`uv run python /tmp/gen_specimen.py` (to be productised as a repo page in
the standalone-system work).

## Fleet verdict — full 12-app round, persona-matched sampling (v0.93.6)

Strict bar (broken `shadcn_cards` reference frames excluded — see
integrity notes):

| Dimension | Baseline gap | Now | Locked margin | Verdict |
|---|---|---|---|---|
| color_discipline | 2.59 | 0.25 | 1.63 | PASS |
| dark_mode_integrity | 1.97 | −0.22 | 1.15 | PASS (ahead) |
| perceived_craft | 3.24 | 0.98 | 0.50 | **FAIL** |
| spatial_rhythm | 2.75 | 0.10 | 0.82 | PASS |
| state_completeness | 2.46 | 0.49 | 0.58 | PASS |
| typographic_hierarchy | 2.44 | 0.61 | 1.15 | PASS |

Per-app pooled gaps vs the reference pool: eight of eleven apps are at or
ABOVE reference level (support_tickets −0.69 leads); stragglers are
ticket_classifier/domain_join_co (+0.49) and hr_records (+0.56) — all
inside every margin except craft.

## What moved it

Phase 2 (v0.93.4): Geist actually loading (the fleet had rendered
system-ui — `--font-sans` named a font no `@font-face` ever served),
140-icon server-side registry, focus-ring tokens, WCAG contrast gate (+
success/warning ramp recalibration). Phase 3 (v0.93.5): nav icons
everywhere (label inference, registry-closed), designed empty states,
badge SVGs, registry row actions, card/button craft. #1536 (v0.93.6):
denial pages designed; persona-matched capture; `dazzle qa login`.

## Integrity notes (read before quoting numbers)

- `ui.shadcn.com/examples/cards` now 404s; its two frames have been in the
  reference pool since the baseline (consistent across rounds, softening
  every round's bar equally). The table above EXCLUDES them; with them
  included all six dimensions pass. Replace the target next round.
- `invoice_ops` (baseline's worst app) is missing from this round — the
  persona-matched plan yielded zero targets for it (#1537). Its absence
  flatters fleet means slightly; margins are not close enough for it to
  flip any verdict except possibly craft, which already fails.
- One acme_billing dark capture rendered light (color-scheme emulation
  race) — scored dark_mode_integrity 2 by a judge; net effect is against
  us, not for us.
- Protocol change vs baseline: persona-matched sampling (the baseline
  document's own recommendation) removed RBAC-denial frames. Part of the
  gap closure is that sampling fix; the Phase 2/3 vehicle checks isolate
  the CSS-driven movement (ops_dashboard 2.22→1.46 under the OLD
  sampling).

## Where this goes — the standalone system

perceived_craft is the binding dimension and its residue is
component-breadth-and-detail work, which is exactly the next scope:
HaTchi-MaXchi extracted as a standalone htmx4-native design system with
shadcn-breadth coverage (spec:
`docs/superpowers/specs/2026-07-03-hatchi-maxchi-standalone-design.md`).
