[project]
name = "fieldtest_hub"
version = "0.1.0"
root = "fieldtest_hub.core"

[modules]
paths = ["./dsl"]

[stack]
name = "dnr"

[ui]
# P17 — typed Page/AppShell Fragment chrome (zero Jinja per request).
fragment_chrome = true

[auth]
enabled = true
provider = "session"
allow_registration = true
