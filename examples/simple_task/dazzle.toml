[project]
name = "simple_task"
version = "0.1.0"
root = "simple_task.core"

[modules]
paths = ["./dsl"]

[stack]
name = "dnr"

[auth]
enabled = false  # Disabled for E2E tests
provider = "session"
allow_registration = true
