# Story walk ST-021 — Team Manager opens assignee overview from the task board
# Maps: stems/story-driven-jobs.md, dsl/stories.dsl ST-021 then:

persona: manager
home_workspace: team_overview

scenes:
  - id: land_st_021
    story: ST-021
    entry: /app/workspaces/team_overview
    actions:
      - type: navigate
      - type: assert_not_login
      - type: assert_http_ok
      - type: assert_any_text
        texts:
          - Assignee
          - Task
          - Member
          - Team
          - Role
    expects: "Authenticated manager sees path cues from tasks to assignee overview hub"
