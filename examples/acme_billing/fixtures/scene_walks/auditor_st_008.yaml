# Story walk ST-008 — maps stems/story-driven-jobs.md + stories.dsl
# Acceptance dig cycle 1514: land-and-see on billing; live dig can deepen hub hops.

persona: auditor
home_workspace: billing

scenes:
  - id: land_st_008
    story: ST-008
    entry: /app/workspaces/billing
    actions:
      - type: navigate
      - type: assert_not_login
      - type: assert_http_ok
      - type: assert_any_text
        texts:
          - Project
          - Invoice
          - Billing
          - queue
    expects: "Auditor sees project/invoice cues before project hub invoice queue"
