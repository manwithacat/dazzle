# Story walk ST-010 — maps stems/story-driven-jobs.md + stories.dsl
# Acceptance dig cycle 1514: land-and-see on my_work; live dig can deepen hub hops.

persona: external_contractor
home_workspace: my_work

scenes:
  - id: land_st_010
    story: ST-010
    entry: /app/workspaces/my_work
    actions:
      - type: navigate
      - type: assert_not_login
      - type: assert_http_ok
      - type: assert_any_text
        texts:
          - Project
          - Invoice
          - Work
          - queue
    expects: "Contractor sees invoice queue cues for project hub hop"
