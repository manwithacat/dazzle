# Story walk ST-002 — maps stems/story-driven-jobs.md + stories.dsl
# Acceptance dig cycle 1514: land-and-see on billing; live dig can deepen hub hops.

persona: auditor
home_workspace: billing

scenes:
  - id: land_st_002
    story: ST-002
    entry: /app/workspaces/billing
    actions:
      - type: navigate
      - type: assert_not_login
      - type: assert_http_ok
      - type: assert_any_text
        texts:
          - Invoice
          - Project
          - Billing
          - portfolio
    expects: "Auditor lands on billing with invoice portfolio cues (read-only review)"
