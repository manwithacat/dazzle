# Stem: Story-driven job home (domain_join_co)

## Claim

Verified-domain join demo still needs a **job-shaped home**: metrics + readiness
strip before the announcement feed.

## Reconstruct

- `home` = announcement metrics + status_list (domain/policy/announcements) + list.
- Join-request queue stays in runtime auth admin — do not invent DSL for it here.
- Persona `default_workspace: home`.

## Not this

- Home = only an announcement list with no join narrative.

## Expressions

- `dsl/domain.dsl` workspace home; `docs/reference/verified-domain-join.md`
