# Story walk ST-004 — Home overview (metrics + favourites queue before full list)
# Maps: stems/story-driven-jobs.md, dsl/stories.dsl ST-004 then:

persona: user
home_workspace: home

scenes:
  - id: land_st_004
    story: ST-004
    entry: /app/workspaces/home
    actions:
      - type: navigate
      - type: assert_not_login
      - type: assert_http_ok
      - type: assert_any_text
        texts:
          - Contact
          - Favourite
          - Find a contact
          - directory
    expects: "Authenticated user sees Home directory metrics and favourites queue cues"
