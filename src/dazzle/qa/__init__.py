"""Dazzle visual QA toolkit — capture, evaluate, and report on rendered app surfaces."""

from dazzle.qa.models import CapturedScreen, Finding, QAReport

__all__ = ["CapturedScreen", "Finding", "QAReport"]
