"""/ux-cycle runtime helpers.

Standalone package for ux-cycle strategy wirings. The top-level /ux-cycle
loop is implemented as a slash command (`.claude/commands/ux-cycle.md`) —
this package holds reusable building blocks the slash command can import
when it dispatches into Python.
"""
