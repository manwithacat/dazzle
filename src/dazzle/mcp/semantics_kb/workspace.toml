# Workspace Concepts
# Auto-generated from semantics.py

[meta]
file = "workspace"
concept_count = 3

[concepts.aggregates]
category = "Workspace Component (v0.2)"
doc_page = "workspaces"
doc_order = 4
definition = """
Computed metrics and aggregate functions for workspace regions.

**Entry point for chart authoring:** read ``docs/reference/reports.md``
before writing any chart region. It explains the single/multi-dim
decision, the fast/slow-path distinction, FK auto-join behaviour,
scope-safety contract, and the ``dazzle db explain-aggregate``
debugger.

Fast path (recommended — one SQL query, scope evaluated once):
- ``count(<source_entity>)`` with ``group_by: <field>`` — bar_chart
- ``count(<source_entity>)`` with ``group_by: [<a>, <b>]`` — pivot_table
- ``sum:<col>``, ``avg:<col>``, ``min:<col>``, ``max:<col>`` — numeric measures

Slow path (N+1 — avoid when the fast path applies):
- ``count(<other_entity> where field = current_bucket)`` — cross-entity,
  per-bucket queries. Used only when the bucketing dimension lives on
  a different entity than what you're counting.
"""
syntax = '''
aggregate:
  total: count(Task)
  completed: count(Task where status = done)
  completion_rate: count(Task where status = done) * 100 / count(Task)
  avg_duration: avg:duration_days
'''
related = ["regions", "workspace", "display_modes"]
v0_2_changes = "NEW in v0.2"
v0_59_changes = "Strategy C aggregate primitive. count(<source>) routes through Repository.aggregate (one GROUP BY query, scope pre-applied). Multi-dim via group_by: [a, b] in v0.59.3. dazzle db explain-aggregate CLI in v0.59.4."

[concepts.display_modes]
category = "Workspace Component (v0.2)"
doc_page = "workspaces"
doc_order = 5
definition = """
Visualization modes for workspace regions. Each display consumes a
specific IR shape:

- ``list`` / ``grid`` / ``timeline`` / ``map`` — row-per-item views
- ``metrics`` / ``summary`` — KPI tiles from an aggregate: block
- ``bar_chart`` / ``funnel_chart`` — single-dim count/measure distribution
  (uses ``group_by: <field>``)
- ``kanban`` — single-dim grouping with column-per-value layout
- ``heatmap`` — 2-dim matrix (``heatmap_rows:`` + ``heatmap_columns:``)
- ``pivot_table`` — N-dim cross-tab (``group_by: [field, field]``) — v0.59.3
- ``tree`` / ``activity_feed`` / ``queue`` / ``progress`` — specialised

**New in v0.59: Strategy C aggregate primitive.** Chart regions now run
one scope-aware GROUP BY SQL query via Repository.aggregate — no more
N+1 enumeration. See the ``reports`` doc for the canonical entry
point when writing any chart region.
"""
syntax = "display: <list|grid|timeline|map|metrics|bar_chart|funnel_chart|pivot_table|heatmap|kanban|tree|queue|tabbed_list|activity_feed|progress>"
related = ["regions", "workspace", "aggregates"]
v0_2_changes = "NEW in v0.2"
v0_59_changes = "pivot_table display added; bar_chart / pivot_table routed through Repository.aggregate (Strategy C)."

[concepts.regions]
category = "Workspace Component (v0.2)"
doc_page = "workspaces"
doc_order = 2
definition = """
Named sections within a workspace that pull data from entities or surfaces.
Each region defines a data view with optional filtering, sorting, display mode,
and aggregation. Three types of regions:
- Data region: has source + optional filter/sort/limit (renders entity rows)
- Aggregate-only region: has only aggregate: block (renders KPI metric cards)
- Multi-source region: has sources: [Entity1, Entity2] (renders tabbed list)

Regions support date-range filtering (v0.34.0) with date_field + date_range flag.
"""
syntax = '''
<region_name>:
  source: <EntityName>                       # Single source
  source: [<Entity1>, <Entity2>]             # Multi-source (tabbed list)
  [filter: <condition_expr>]                 # Row filter
  [filter_map:]                              # Per-source filters (multi-source)
    [<EntityName>: <condition>]
  [sort: <field> [asc|desc], ...]            # Sort order
  [limit: <1-1000>]                          # Max records
  [display: <list|grid|timeline|map|detail|summary|metrics|kanban|bar_chart|funnel_chart|queue|tabbed_list>]
  [action: <surface_name>]                   # Click-through surface
  [empty: "<message>"]                       # Empty state message
  [group_by: <field>]                        # Group data by field
  [date_field: <field>]                      # Date-range filter field
  [date_range]                               # Enable date picker
  [aggregate:]                               # KPI metrics
    [<metric_name>: <expression>]
'''
example = '''
workspace ops_dashboard "Operations Dashboard":
  purpose: "Monitor daily operations"

  # Data region with filter and sort
  open_tickets:
    source: Ticket
    filter: status = open
    sort: priority desc, created_at asc
    limit: 20
    display: list
    action: ticket_detail
    empty: "No open tickets"

  # Aggregate-only region (no source needed)
  kpi_metrics:
    aggregate:
      total_tickets: count(Ticket)
      open_count: count(Ticket where status = open)
    display: metrics

  # Detail region (single record view)
  current_user_profile:
    source: UserProfile
    display: detail

  # Multi-source tabbed region
  activity:
    source: [Ticket, Order, Invoice]
    sort: created_at desc
    limit: 10
    display: tabbed_list

  # Date-range filtered region
  sales:
    source: Order
    date_field: created_at
    date_range
    sort: created_at desc
    display: list
'''
related = ["workspace", "display_modes", "aggregates"]
v0_2_changes = "NEW in v0.2"
v0_33_0_changes = "Added multi-source regions (tabbed_list) and queue display mode"
v0_34_0_changes = "Added date_field and date_range for date-range filtering"
