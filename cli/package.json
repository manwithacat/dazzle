{
  "name": "@dazzle/cli",
  "version": "0.11.0",
  "description": "DAZZLE CLI - Fast, LLM-friendly command line interface",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "dazzle": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile=dist/dazzle",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  }
}
