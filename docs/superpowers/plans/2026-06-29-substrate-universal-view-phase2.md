# Phase 2 — VIEW: make the substrate the universal detail render path, retire `render_detail_view`

> **For agentic workers:** execute task-by-task (superpowers:executing-plans), Hybrid mode — inline TDD + an independent adversarial review before the flip (Task 5) and the delete (Task 6). Follows the exact arc proven in Phase 1 (`2026-06-29-substrate-universal-list-phase1.md`, shipped v0.92.15–19).

**Goal:** Every `mode: view` (detail) surface renders through the typed Fragment substrate (`_build_view`); then **delete** the legacy `render_detail_view` (~635 LOC, `page/runtime/detail_renderer.py`) and repoint its callers, with no fallback (ADR-0049 D4).

**Decisions (inherited, locked):** D1 visual—not byte—parity (substrate DOM canonical; re-baseline + gate on `dazzle ux verify` / card-safety composite / a11y). D2 N/A for view (no skeleton/hydrate — detail is server-rendered inline). D4 no silent legacy fallback post-delete. D5 view is this phase; create/edit is Phase 3.

## Step-0 gap-diff (legacy `render_detail_view` vs substrate `_build_view`)

Substrate `_build_view` already has: fields (Row of Heading4+Text), an action toolbar via `_build_detail_actions` (Edit/transitions/integration/external/Delete), related-group regions (as **Skeleton placeholders**). Gaps:

| Element | Legacy | Substrate | Disposition |
|---|---|---|---|
| **RBAC action anchors** | `data-dazzle-action="{entity}.edit/.delete/.transition.{s}/.external.{n}/.integration.{i}.{m}"` on each action | plain `Link`/`Button`, **no `data-dazzle-action`** | **BUILD** — the RBAC contract checker keys off these; without them the detail loses RBAC verification (parallels Phase 1's CreateButton anchor). |
| **htmx wiring on actions** | Delete `hx-delete`+`hx-confirm`, transitions `hx-put`+`hx-vals`, external `target=_blank` | Delete has hx_*; transitions Button has hx_put? (verify); external = plain Link | **VERIFY/BUILD** — confirm transition `hx-vals` (status_field→to_state) + external new-tab. |
| **Back button** | header Back `<a>` with drawer-close/history-back JS | none | **DECISION** — build a substrate Back affordance, or drop (peek/drawer close is separate). Flag for review. |
| **Delete confirm semantics** | `hx-confirm` | Button hx_confirm | OK (verify). |
| **Related groups** | actual related content (verify what legacy renders) | **Skeleton(lines=3) placeholder** | **DECISION** — legacy related-group content vs substrate placeholder. If legacy renders real content, this is a real gap; if it's also a placeholder, parity holds. Verify in Task 1. |
| **`<dt>/<dd>` field structure** | `dz-detail` definition list | Row(Heading4, Text) | substrate-canonical (D1) — re-baseline. |
| **peek `?peek=1` detail-body** | peek 4a (v0.92.14) serves the legacy detail **body** partial | — | **CRITICAL** — peek's content-only body currently rides the legacy path; converging detail must keep peek working (serve the substrate detail body for `?peek=1`). Trace `is_peek_request`/`_render_response` peek branch + `resolve_peek_mode`. |

**Callers of `render_detail_view`** (grep at Task 6): `template_renderer._render_body_inner` (detail branch — the sole legacy detail producer + the D4 loud-error target), `experience_renderer` (the detail STEP — same `page ↛ http` repoint as Phase 1's table-step, via the http experience route), the **peek** body partial path, and any `_render_response`/`pdf_viewer` seam. **Sweep `render_detail_view(` / `render_page(` for detail ctx — Phase 1's lesson: the E2E (PostgreSQL) tier catches missed callers the unit suite can't (build-ui/serve/fidelity).**

## Tasks

- [ ] **Task 1 — Characterize** the legacy detail chrome (`render_detail_view`) across a matrix (no-actions / edit+delete / transitions / external / integration / related-groups / empty) → committed fixtures `tests/unit/__snapshots__/legacy_detail_chrome/` (visual-parity reference; exclude dir from whitespace/eof pre-commit hooks). Verify the related-groups + peek-body shapes here.
- [ ] **Task 2 — RBAC anchors + action htmx parity:** add `data-dazzle-action` to the substrate detail actions (Edit/Delete/transition/external/integration) + confirm transition `hx-put`/`hx-vals` + external new-tab. New primitive fields or a `DetailAction` sidecar; TDD. (Mirrors Phase 1's CreateButton/anchor work.)
- [x] **Task 2 — DONE (shipped v0.92.20):** `data-dazzle-action` anchors on all detail actions (analytics + gesture selectors), transitions fixed to `hx-put`+`hx-vals` (status_field→to_state — the prior `hx-post` without vals was a real transition break), external `new_tab`→`target=_blank`; dispatch ctx threads `status_field`+integration `name`/`mapping`+external `name`; new `Button.data_action`/`Link.data_action`/`Link.new_tab`.
- [ ] **Task 3 — Remaining chrome gaps + RELATED-GROUPS (James: "reproduce now"):**
  - [x] **3a Related-groups — DONE (shipped v0.92.21):** all 3 display modes (table/status_cards/file_list) render real content; dispatch ctx threads the fetched `detail.related_groups`; new pure `RelatedGroup`/`RelatedTab` primitives + adapter `_build_related_group` (cells via `_format_cell`). Investigated 2026-06-29: the substrate renders related groups as `Skeleton` placeholders; legacy renders **real inline content** (3 display modes: `table`/`status_cards`/`file_list`) used by 4+ example apps (simple_task, project_tracker, support_tickets, fieldtest_hub, design_studio). **The substrate dispatch ctx doesn't even carry the data** — it threads `surface.related_groups` (IR config: name/title/display only), NOT `detail.related_groups` (the fetched `RelatedGroupContext` with `tabs[RelatedTabContext]` carrying columns/rows/total/detail_url_template/create_url/filter_field/filter_type_*). Plan:
    1. Thread `render_ctx.detail.related_groups` (the fetched groups) through `_build_dispatch_ctx` (replace the surface-IR-only `related_groups_out`).
    2. New substrate primitives `RelatedGroup` + `RelatedTab` (pure render/). The adapter pre-formats each row's cells via `_format_cell` (typed: badge/date/currency/bool) → plain cell strings on `RelatedTab`, keeping the renderer pure + the primitive simple.
    3. Renderer handles all 3 modes: shared per-tab create-row (`<a data-dazzle-action="{entity}.create">+ New {label}</a>`) + visible-gate; **table** = Alpine tab-strip (`x-data activeTab`, multi-tab) + per-tab `dz-related-table` (thead from columns + `<tr hx-get>` drill rows); **status_cards** = per-tab cards (first 3 cols as text lines, primary/secondary); **file_list** = per-tab file rows (first 2 cols). Reference: legacy `detail_renderer._render_related_table_group`/`_render_related_status_cards`/`_render_related_file_list`/`_render_create_row`/`_render_related_table_cell`. Cell typing helpers live in `dazzle.render.filters` (render layer — reusable).
    4. Wire `_build_view` to emit `RelatedGroup`s instead of `Skeleton`; TDD per mode; re-baseline.
  - [x] **3b mechanical gaps — DONE (shipped v0.92.22):** detail Region carries `data-dazzle-entity`/`data-dz-entity`/`data-dz-entity-id` (the tier2 e2e gesture selector + dz-analytics consumer — the E2E tier would've caught this at the flip); audit-history slot (`dz-detail-audit-history` hx-get `/_dazzle/audit-history/{e}/{id}` on `show_history`, threaded); `← Back` link from `back_url`. ORIGINAL: Back affordance (decision: build a substrate Back, or drop — peek/drawer-close is separate), `dz-detail`/`dz-detail-wide` wrapper class + `data-dazzle-entity`/`data-dz-entity`/`data-dz-entity-id` attrs on the detail Region, the audit-history slot (`dz-detail-audit-history` hx-get `/_dazzle/audit-history/{entity}/{id}` when `show_history`). TDD per element.
- [x] **Task 4 — peek convergence: SUBSUMED BY THE FLIP (traced 2026-06-29).** The peek path (`page_routes.py:2059-2061`) renders via `render_page(content_only=True, inner_html=inner_html)` where `inner_html = _maybe_dispatch_inner_html(...)` — the SAME dispatch as the regular view render; there is no separate peek detail-body renderer (the peek chevron just `hx-get`s `<detail-url>?peek=1`, hitting the VIEW route content-only). So the Task-5 flip makes peek serve the substrate detail body automatically. NOTE (polish, non-blocking): the substrate peek body includes the `Surface` header (h1 + Back); a content-only peek could omit it (a `_build_view` peek-mode flag) if the nested `dz-surface`-in-panel proves heavy — defer unless the flip review flags it.
- **Task 5 — FLIP (independent review):**
  - [x] **Step 0: independent adversarial review — DONE; findings ALL FIXED + shipped v0.92.23.** The review found real fleet-breakers (as in Phase 1): SEV-1 field values regressed (ref→raw UUID, money/currency→raw minor units, badge→lost WCAG chrome, bool→text, file→lost link) → converged onto `_render_cell_display` + threaded the missing ctx; SEV-2 peek would inject `dz-surface`+h1+Back into a `<td>` → `_build_view` peek-mode (`ctx["peek"]`); SEV-3 `data-dz-action` analytics → restored. **Review also flagged Task-6 (delete) blockers** (for when the legacy is removed): the missed callers `static_preview` VIEW branch (build-ui), `fidelity`, the `experience_renderer` detail-STEP (page↛http), `template_renderer._render_body_inner` detail branch (D4 loud error) — the detail equivalents of Phase 1's list seams. Address in Task 6.
  - [x] **Step 1: the flip — DONE (v0.92.24)**: — `_maybe_dispatch_inner_html` dispatches `mode: view` when `render is None`; ALSO set `ctx_dict["peek"] = is_peek_request(prc.request)` so peek renders content-only. (View/create-edit gate becomes list+view; create/edit stay legacy.)
  - [x] **Step 2: full suite — green (19291), NO view-render churn** (detail pages 404 without records, so no test exercised the full default-view render via render_page — same as the list flip). Only the deferred-imports ratchet adjusted (htmx imports hoisted, page_routes 25→24). — re-baseline churned view-rendering tests (inspect; the list flip churned ~10 test files — expect similar for view: dispatch_ctx_detail_*, fragment_surface tests, peek tests, runtime_pipeline/page_purpose detail vehicle).
  - [x] **Step 3: oracles — substrate detail verified: 0 card-safety violations (peek + non-peek), peek=content-only (0 h1, no dz-surface), badge/money/related render; composite green.** Legacy stays as fallback until Task 6. — card-safety + structural scanners + verify live (TestClient boot a default-render VIEW page → substrate). CI runs ux verify/INTERACTION_WALK/GUIDE_WALK. Ship.
- [ ] **Task 6 — DELETE (scoped 2026-06-29; the view flip is shipped + CI-green, so this is the final cleanup).** `render_detail_view` (~635 LOC: `render_detail_view` + `_render_field_value` + `_render_status_badge` + `_render_create_row` + `_render_related_*` + `_render_related_table_cell` — likely the whole `detail_renderer.py` module). Callers + the seven steps:
  1. **#1297 DELEGATION (the wrinkle the flip-review missed):** `render_detail_view` is re-exported from `page/runtime/__init__.py` (`__all__`) and used by `fixtures/custom_renderer/app/render/feedback_detail.py` (`from dazzle.page.runtime import render_detail_view; render_detail_view(detail)`) — the #1297 "custom VIEW renderer delegates to the framework's generic detail" contract. **Preservable:** the custom renderer's `render(self, surface, ctx)` already has `surface`+`ctx`, so provide a substrate-backed public helper `render_generic_detail(surface, ctx)` (= `FragmentRenderer().render(FragmentSurfaceAdapter()._build_view(surface, ctx))`) and repoint the fixture + the #1297 test (`test_dispatch_ctx_detail_view.py:185-194`) to it. Remove the `render_detail_view` re-export from `page/runtime/__init__`.
  2. **template_renderer._render_body_inner detail branch** (`:77-79`) → loud `RuntimeError` (D4), like the list branch.
  3. **experience_renderer detail-STEP** (`:224-226`, page↛http) → repoint via the http experience route's pre-render (extend the Phase-1 `table_step_html` pattern to a `detail_step_html`).
  4. **static_preview VIEW branch (build-ui)** → inject a detail-body renderer (extend `generate_preview_files_with_substrate` / the `list_body_renderer` seam to VIEW).
  5. **fidelity** → already renders lists via substrate; extend `_render_for_fidelity` to dispatch VIEW too (it does already — verify it covers detail post-delete).
  6. Delete `detail_renderer.py`; migrate tests (delete `test_legacy_detail_chrome_char_phase2` + snapshots; fix the related-groups test's comment ref); regen complexity/clone/deferred baselines.
  7. Full suite + lint/mypy/import-linter (`render is pure` + `page ↛ http` KEPT) + **monitor the E2E (PostgreSQL) tier** (it caught Phase 1's missed detail-equivalent callers).
- [ ] **Task 7 — ADR/CHANGELOG/ship.** ADR-0049 status → Phase 2 (view) shipped. CHANGELOG + Agent Guidance. `/bump`, push, CI green.

## Phase 2 gate
- [ ] Every `mode: view` surface renders via the substrate; peek body served by the substrate; `render_detail_view` deleted; full suite + card-safety + a11y + the CI e2e/browser tier green; `render is pure` + `page ↛ http` KEPT; goldens re-baselined with inspected diffs.
