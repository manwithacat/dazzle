# R3 peer pack — Goal B context for support_tickets

app = "support_tickets"
peer = "Zendesk / Front / Intercom agent workspace"
domain = "customer_support"

above_fold = [
  "Live conversation or comment trail on the ticket — not queue meta alone",
  "Priority / SLA / assignee without leaving the desk",
  "Multi-panel attention (critical + unassigned) if claiming command density",
  "People / org only when it helps routing — not a decorative Team desk clone",
]

refuse = [
  "Re-panel acceptance without product fix",
  "Headshot shelf as the only media claim when tickets have no creative assets",
  "Funnel chart theater on Manager Ops",
  "Enum × ball_in_court cartesian slices (thankful/medium/channel × needs_reply)",
  "Denormalized parent enums on Comment (case_priority, sla_pressure) so count() can filter",
  "Another display: conversation region on an already-saturated ticket desk",
  "Customers / requesters in the Agent Console context_selector (staff-only picker)",
]

icon_depths = ["conversation", "command_density", "empty_region_honesty"]
surprise_prompts = [
  "Can a lead work one waiting-on-you trail and the live ticket thread without a filter wall?",
  "Does the first screen look like a real queue a lead would work — not 19 conversation slices?",
]
