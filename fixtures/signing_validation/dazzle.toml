[project]
name = "signing_validation"
title = "Signing Validation Fixture"
description = "Minimal signable app for signing harness integration tests"
root = "signing_validation"
version = "0.1.0"
type = "benchmark"

[modules]
paths = ["./dsl"]

[stack]
name = "dnr"

[auth]
enabled = true
provider = "session"
allow_registration = false

[signing]
project_name = "Test Co"
