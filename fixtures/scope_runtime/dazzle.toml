[project]
name = "scope_runtime"
version = "0.1.0"
root = "scope_runtime.core"

[modules]
paths = ["./dsl"]

[stack]
name = "dnr"

[auth]
enabled = true
provider = "session"
allow_registration = false
